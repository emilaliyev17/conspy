# Инструкция по локальному запуску и деплою

## 🚀 Локальный запуск

### Шаг 1: Настройка окружения

1. **Перейдите в директорию проекта:**
```bash
cd /Users/emil.aliyev/Projects/Conspy/conspy
```

2. **Создайте файл `.env` из примера:**
```bash
cp .env.example .env
```

3. **Отредактируйте `.env` файл:**
   - Установите `DEBUG=True` для разработки
   - Добавьте `SECRET_KEY` (можно сгенерировать новый)
   - Настройте подключение к базе данных (см. варианты ниже)

### Шаг 2: Настройка базы данных

**Вариант A: Использовать продакшн базу (для тестирования с реальными данными)**
- Раскомментируйте и заполните параметры БД в `.env`
- Используйте те же credentials, что и в продакшене

**Вариант B: Использовать локальную SQLite (для разработки без продакшн данных)**
- Создайте файл `local_settings.py` в `financial_consolidator/`:
```python
# local_settings.py
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```
- Или измените `settings.py` временно для локальной разработки

### Шаг 3: Активация виртуального окружения

```bash
# Активируйте существующее venv
source venv/bin/activate

# Или создайте новое (если нужно)
python3 -m venv venv
source venv/bin/activate
```

### Шаг 4: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 5: Применение миграций

```bash
python manage.py migrate
```

### Шаг 6: Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

### Шаг 7: Сбор статических файлов

```bash
python manage.py collectstatic --noinput
```

### Шаг 8: Запуск сервера разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

---

## 📤 Деплой в продакшн (DigitalOcean App Platform)

### Процесс деплоя:

1. **Убедитесь, что все изменения закоммичены:**
```bash
git status
git add .
git commit -m "Описание изменений"
```

2. **Запушьте в GitHub:**
```bash
git push origin main
```

3. **DigitalOcean автоматически:**
   - Обнаружит новый коммит
   - Запустит сборку Docker образа (используя `Dockerfile`)
   - Применит миграции (если настроено)
   - Перезапустит приложение

### Важные моменты для продакшена:

1. **Переменные окружения в DigitalOcean:**
   - Убедитесь, что все необходимые переменные установлены в панели DigitalOcean
   - `SECRET_KEY` - должен быть установлен
   - `DEBUG=False` - для продакшена
   - `DATABASE_URL` - подключение к продакшн БД
   - `ALLOWED_HOSTS` - домен вашего приложения

2. **Миграции:**
   - DigitalOcean может автоматически применять миграции при деплое
   - Или можно применить вручную через консоль: `python manage.py migrate`

3. **Статические файлы:**
   - Собираются автоматически при сборке Docker образа (строка 24 в Dockerfile)
   - Используется WhiteNoise для раздачи статики

4. **Проверка после деплоя:**
   - Проверьте логи в DigitalOcean панели
   - Убедитесь, что приложение запустилось
   - Проверьте работу основных функций

---

## 🔧 Полезные команды

### Локальная разработка:
```bash
# Запуск сервера на другом порту
python manage.py runserver 8001

# Создание новой миграции
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Запуск Django shell
python manage.py shell

# Проверка настроек
python manage.py check
```

### Работа с Git:
```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Добавить файлы
git add .

# Закоммитить
git commit -m "Описание изменений"

# Запушить
git push origin main

# Посмотреть историю
git log --oneline
```

---

## ⚠️ Важные замечания

1. **Никогда не коммитьте `.env` файл** - он уже в `.gitignore`

2. **Проверяйте перед коммитом:**
   - Нет ли хардкодных паролей/ключей
   - Нет ли отладочного кода (print, console.log)
   - Все миграции применены локально

3. **Тестируйте локально перед пушем:**
   - Запустите сервер
   - Проверьте основные функции
   - Убедитесь, что нет ошибок в консоли

4. **После деплоя:**
   - Проверьте логи в DigitalOcean
   - Протестируйте основные функции на продакшене
   - Убедитесь, что миграции применены

---

## 🐛 Решение проблем

### Проблема: Не могу подключиться к БД
- Проверьте параметры в `.env`
- Убедитесь, что БД доступна (для продакшн БД проверьте firewall)
- Проверьте SSL режим для PostgreSQL

### Проблема: Ошибки при collectstatic
- Убедитесь, что все статические файлы на месте
- Проверьте права доступа к директориям

### Проблема: Миграции не применяются
- Проверьте подключение к БД
- Убедитесь, что все миграции созданы (`makemigrations`)
- Проверьте логи Django

### Проблема: После деплоя приложение не работает
- Проверьте логи в DigitalOcean
- Убедитесь, что все переменные окружения установлены
- Проверьте, что `DEBUG=False` в продакшене
- Убедитесь, что `ALLOWED_HOSTS` содержит ваш домен

